# @flowdown/core

`@flowdown/core` is the headless pure-computation package for Flowdown.

It transforms streaming Markdown strings into HAST structures so rendering packages can perform their own framework-specific rendering work. The package intentionally avoids UI and React dependencies, keeping the Markdown pipeline portable, testable, and suitable for high-throughput LLM streaming flows.

## License

MIT
