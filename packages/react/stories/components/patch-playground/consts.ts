export const PATCH_MARKDOWN = `# Live patch workshop

This sentence has a point marker after this word: beacon.

The replacement sample preserves Patch.

A keyless patch can decorate this final sentence while the document streams.
`;

export const POINT_ANCHOR = 'beacon';

export const REPLACEMENT_ANCHOR = 'Patch';

export const KEYLESS_ANCHOR = 'final sentence';
